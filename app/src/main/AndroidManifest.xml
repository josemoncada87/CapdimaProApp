<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.icesi.gleo.capdimaproapp">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".InitialHomeSettingsActivity" />
        <activity android:name=".NeighborsDefinitionActivity" />
        <activity android:name=".CreateSpacesActivity" />
        <activity android:name=".FecadeSelectionActivity" />
        <activity android:name=".OpenHouseActivity"></activity>
    </application>

</manifest>